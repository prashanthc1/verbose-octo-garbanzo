// 🐦 Flutter imports:
import 'package:flutter/material.dart';

const primeryColor = Color(0xFF2697FF);
const seconderyColor = Color(0xFF2A2D3E);
const bgColor = Color(0xFF212332);

const defaultPadding = 16.0;
